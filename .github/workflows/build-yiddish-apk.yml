name: Build Yiddish Learning APK

on:
  push:
    branches: [ "claude/ui-ux-pro-max-install-bucdzh" ]
    paths:
      - 'yiddish-app/**'
      - '.github/workflows/build-yiddish-apk.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK components
        run: |
          sdkmanager "platforms;android-34" "build-tools;34.0.0" "platform-tools"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: yiddish-app
        run: npm install

      - name: Sync Capacitor
        working-directory: yiddish-app
        run: npx cap sync android

      - name: Set execute permission on gradlew
        working-directory: yiddish-app/android
        run: chmod +x gradlew

      - name: Build debug APK
        working-directory: yiddish-app/android
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}
        run: ./gradlew assembleDebug --no-daemon

      - name: Rename APK
        run: |
          cp yiddish-app/android/app/build/outputs/apk/debug/app-debug.apk \
             yiddish-learning-v1.0.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: Yiddish-Learning-APK
          path: yiddish-learning-v1.0.apk
          retention-days: 30

      - name: Show APK info
        run: |
          ls -lh yiddish-learning-v1.0.apk
          echo "✅ Yiddish APK built successfully!"
          echo "📱 Download from Actions → Artifacts"
